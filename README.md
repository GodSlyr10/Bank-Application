# Version 1

Download ollama for your os using https://ollama.com/download.

create a venv using python3 -m venv .venv

install the dependencies

run generate_vector_db first to create a vector database

then run agent.py to start running the agent.